{
  "mcpServers": {
    "zerodha": {
      "command": "npx",
      "args": ["-y", "kite-connect-mcp"],
      "env": {
        "KITE_API_KEY": "${ZERODHA_API_KEY}",
        "KITE_ACCESS_TOKEN": "${ZERODHA_ACCESS_TOKEN}"
      }
    }
  }
}
